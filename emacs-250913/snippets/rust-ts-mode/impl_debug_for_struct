# -*- mode: snippet -*-
# name: impl_debug_for_struct
# key: impl_debug_for_struct
# --
impl std::fmt::Debug for ${1:struct_name} {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        // write!(f, \"{}\", self.xxx)
        // f.debug_struct(\"My Test Struct\")
        // .field(\"name\", &self.name)
        // .finish()
        $0
    }
}